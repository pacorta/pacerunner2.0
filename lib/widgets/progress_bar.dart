import 'package:flutter/material.dart';

import 'pace_bar.dart';

class paceShow extends StatelessWidget {
  const paceShow({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: PaceBar(),
    );
  }
}
